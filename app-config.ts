import { TimeFilter } from "./shared/schemas";

const config = {
  categories: ["technology", "finance", "languages", "arts", "legal", "other"],
  timeFilters: Object.values(TimeFilter)
};

export default config;
